\documentclass[$if(fontsize)$$fontsize$$else$10pt$endif$, margin]{res}
\setlength{\textwidth}{5.1in}
\usepackage{helvet}
\usepackage[utf8]{inputenc}
\usepackage[colorlinks=true, linkcolor=blue, $if(emailcolor)$urlcolor=$emailcolor$$else$urlcolor=blue$endif$]{hyperref}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}

$for(name)$
\moveleft.5\hoffset\centerline{\large\bf $name$}
$endfor$

\moveleft\hoffset\vbox{\hrule width\resumewidth height 1pt}\smallskip % Horizontal line after name; adjust line thickness by changing the '1pt'

$if(adress)$
\moveleft.5\hoffset\centerline{$adress$}
$endif$
$if(postcode)$
\moveleft.5\hoffset\centerline{$postcode$}
$endif$
$if(phone)$
\moveleft.5\hoffset\centerline{$phone$}
$endif$
$if(email)$
\moveleft.5\hoffset\centerline{$email$}
$endif$
\begin{resume}

$for(include-before)$
$include-before$
$endfor$



$body$

$for(include-after)$
$include-after$

$endfor$
\end{resume}
\end{document}
